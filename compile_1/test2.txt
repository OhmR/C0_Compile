const int cInt1 = 003;
const int cInt2 = +10000, cInt3 = -9999, cInt4 = 233;
const char cChar1 = 'a';
const char cChar2 = '_', cChar3 = 'b', cChar4= 'B';

int temp;
int arr_size, arr[5];
char string[5];

void print_info{ 
    printf(" !#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\]^_`abcdefghijklmnopqrstuvwxyz{|}");
    printf(cInt1);
    printf("hello,world! ", cInt2+cInt3+cint4+TEMP);;
}
void change_temp(int temp_a, char temp_b){ 
    temp = ((temp_a * (temp_a * cint2 + temp_b) + arr[cint2 + cint3] ) * cint4 + cchar + cChar3) / ((2+1)/3);
    temp = temp_a * (temp_a + temp_b);
    printf(temp);
}

void for_test{ 
    int iter;
    iter = 0;
    for(iter = 0; iter > 100; iter = iter + 1){
        printf(iter);
    }
}

char string_query{
    return(string[cInt1/(cInt2+cInt3)]);
}

int add(int temp_a, int temp_b){
    const int cINT5 = 5;
    int a, b, sum;
    a = temp_a;
    b = temp_b;
    sum = 0;
    if(a >= cInt5){
        return(sum);
    }
    if(b >= cInt5) return(0); 
    else{
        sum = arr[a] + arr[b];
        return(sum);
    }
}

int fibo(int n){
    if(n < 0){
        printf("error");
        return;
    }
    if(n == 0) return(1);
    if(n == 1) return(1);
    return (fibo(n-1) + fibo(n-2));
}

void main(){
    int iter, itmp;
    int choose;
    char ctmp;

    iter = 0;
    arr_size = 5;
    for_test;
    while(iter < arr_size){ 
        scanf(itmp);
        arr[iter] = itmp;
        iter = iter + 1;
    }
    for(Iter=0;ItEr < arr_size; iter = iteR + 1){ 
        scanf(ctmp);
        string[iter] = ctmp;
    }
    printf(string_query); 
    for(ITER=4; ITer >= 0; iter = iter - 2){ 
        printf(string[iter]);
    }

    scanf(choose);
    if(choose == 2){
        change_temp(5+3*4, '+');
        print_info;
    }
    else if(choose > 2){ 
        printf(add(10, 2));
        printf(add(2, 10));
        printf(add(2, 3));
    }
    else{  
        printf(fibo(-1));
        printf(fibo(0));
        printf(fibo(1));
        printf(fibo(3));
    }
}